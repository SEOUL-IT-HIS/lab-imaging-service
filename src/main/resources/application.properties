spring.application.name=lab-imaging-service

spring.datasource.url=jdbc:oracle:thin:@seoulit.pe.kr:1521/freepdb1
spring.datasource.username=lab_imaging
spring.datasource.password=labimaging
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

server.port=8085

springdoc.swagger-ui.path=/swagger-ui.html

# ---------------------------------------------------------------------------
# 세션 저장소 (Redis) — 서비스 간 로그인 공유 (2026-09-16 admin 팀 가이드)
#
# ⚠ Redis 자체는 원격 서버(192.168.1.126)에서 이미 24시간 돌고 있다. 개인 PC에서
#   lab-imaging을 띄울 때도 이 주소 그대로 써야 admin과 세션이 공유된다(개인 PC용으로
#   바꾸면 혼자만의 세션이 되어 admin 로그인이 공유되지 않는다).
# ⚠ REDIS_PASSWORD는 setx로 미리 걸어둔 OS 환경변수여야 한다(값은 팀에서 별도 전달).
#   설정 안 하면 로그인 자체가 막힌다 — IntelliJ도 setx 이후 재시작해야 인식한다.
# ---------------------------------------------------------------------------
spring.data.redis.host=192.168.1.126
spring.data.redis.port=6379
spring.data.redis.password=${REDIS_PASSWORD:}

# 서버 세션 (JWT 미사용). timeout은 서비스마다 다르면 사람마다 끊기는 시점이 달라지므로
# 팀 전체가 10h로 통일한다.
server.servlet.session.timeout=10h
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.path=/
server.servlet.session.cookie.same-site=lax

# ---------------------------------------------------------------------------
# 타 서비스 연동 (Kafka 도입 전 단계 — REST 직접 호출)
# 팀원별로 각자 로컬 PC에서 서버를 띄우므로 host(IP)가 서로 다르다.
# 기본값은 프론트 next.config.ts 의 rewrite origin 과 동일하게 맞춰뒀고,
# 본인 환경이 다르면 환경변수로 이 property 하나만 덮어쓴다.
#
# patient-service 주소. host+port 두 값 대신 base-url 하나로 합쳤다(2026-09-15, admin과 동일).
#   ① 본인 PC + patient도 본인 PC     : 기본값 그대로 (192.168.1.149:8087)
#   ② 본인 PC + patient는 docker compose: IntelliJ 실행구성 환경변수에
#        APP_PATIENT_SERVICE_BASE_URL=http://192.168.1.126:18087 추가 (개인 설정, 커밋 금지)
#   ③ lab-imaging 자신도 같은 docker compose 컨테이너로 뜰 때: compose 쪽에서
#        APP_PATIENT_SERVICE_BASE_URL=http://patient:8087 을 environment 로 주입
#        ⚠ 현재 docker-compose(1).yml은 이 이름이 아니라 APP_PATIENT_SERVICE_HOST/PORT를
#          두 줄로 주고 있다 — base-url로 바꾼 이상 compose 담당자에게 이름 변경을 알려야 한다.
# ---------------------------------------------------------------------------
app.patient-service.base-url=http://192.168.1.149:8087

# admin-service 주소. host+port 두 값 대신 base-url 하나로 합쳤다(2026-09-15).
# 기본값은 "본인 PC끼리 테스트"할 때 값이다. 상황별로 바꿔야 하면 아래처럼 이 property 하나만
# 덮어쓰면 된다(host/port 두 줄을 따로 맞출 필요 없음) — Spring 관례상 환경변수 이름은
# APP_ADMIN_SERVICE_BASE_URL (하이픈 -> 밑줄).
#   ① 본인 PC + admin도 본인 PC     : 기본값 그대로 (192.168.1.128:8080)
#   ② 본인 PC + admin은 docker compose: IntelliJ 실행구성 환경변수에
#        APP_ADMIN_SERVICE_BASE_URL=http://192.168.1.126:18080 추가 (개인 설정, 커밋 금지)
#   ③ lab-imaging 자신도 같은 docker compose 컨테이너로 뜰 때: compose 쪽에서
#        APP_ADMIN_SERVICE_BASE_URL=http://admin:8080 을 environment 로 주입
#        ⚠ 현재 docker-compose(1).yml은 이 이름이 아니라 APP_ADMIN_SERVICE_HOST/PORT를
#          두 줄로 주고 있다 — base-url로 바꾼 이상 compose 담당자에게 이름 변경을 알려야 한다.
app.admin-service.base-url=http://192.168.1.128:8080

# 공통코드 로컬 캐시 — @Scheduled 갱신 주기 (10분)
app.admin-service.common-code.refresh-interval-ms=600000

# ---------------------------------------------------------------------------
# SeaweedFS 연동 (ZP2-21 촬영/영상판독대기등록)
# Filer HTTP API를 직접 호출한다. 별도 SDK 의존성을 추가하지 않는다.
#
# ⚠ 2026-09-09 부터 각자 로컬에서 띄우지 않고, 원격 실험용 서버(Docker) 하나를 함께 쓴다.
#   (이전: weed server -dir=./seaweed-data -filer=true 를 로컬에서 직접 기동)
#   포트는 SeaweedFS 기본 Filer 포트(8888) 그대로라고 가정했다 — Docker 쪽에서 다르게
#   포트매핑했다면 이 값만 바꾸면 된다.
#
# ⚠ 이 값이 연결 안 되는 상황(원격 서버 재기동 등)은 여전히 정상적인 예외 시나리오다.
#   ImageFileService 가 RestClientException 을 LAB055 로 변환해 500 이 아니라
#   400 대의 안내 메시지로 내려준다. (GlobalExceptionHandler 참고)
# ---------------------------------------------------------------------------
app.seaweedfs.filer-url=http://192.168.1.126:8888

# ---------------------------------------------------------------------------
# Kafka 연동 — 처방코어(OPD) <-> 검사(LAB)
# 기동 순서: 브로커 -> 백엔드. 반대로 하면 Consumer 가 연결 재시도 로그를 계속 뱉는다.
#
# 2026-09-09: 원격 실험용 서버(Docker)에 브로커가 올라와 app.kafka.enabled 를 다시 true 로
# 되돌렸다 — 원래 이 값은 "평상시에는 항상 true"가 기본이고, 로컬에 브로커가 없어 지금까지
# false 로 꺼뒀던 것뿐이다. (아래 주석은 되돌릴 수단으로서의 용도는 그대로 유효하다)
#
# 시연 중 Kafka 경로가 막히면 app.kafka.enabled 만 false 로 바꾸고 재기동하면
# Consumer/Producer 빈이 생성되지 않아 기존 REST intake 경로만 남는다.
# (끄기 위한 플래그이자 되돌릴 수단이다)
# ---------------------------------------------------------------------------
app.kafka.enabled=true
spring.kafka.bootstrap-servers=192.168.1.126:9092

app.kafka.topic.lab-order-requested=opd.lab-order.requested.v1
app.kafka.topic.lab-order-resulted=lab.lab-order.resulted.v1
app.kafka.consumer-group.lab-order-requested=lab-service.lab-order-requested-consumer

# 검사결과 확정 -> 수납 청구 이벤트 (수납팀과 확정된 토픽명, 임의 변경 금지)
app.kafka.topic.billing-charge=exam-billing-charge

# 브로커가 없어도 기동 자체는 되도록 메타데이터 대기 시간을 짧게 둔다.
# (시연 당일 브로커를 늦게 띄우는 상황 대비 — 재시도 로그는 남지만 앱은 죽지 않는다)
spring.kafka.producer.properties.max.block.ms=5000

# 발행 직렬화. 값은 JSON, 키(prescriptionId)는 문자열이다.
# ⚠ JacksonJsonSerializer 다. JsonSerializer 가 아니다.
#   둘은 쓰는 Jackson 이 다르다 — JsonSerializer 는 com.fasterxml(Jackson 2),
#   JacksonJsonSerializer 가 tools.jackson(Jackson 3)이고 Boot 4 는 이쪽이다.
# ⚠ 이 두 줄이 없으면 Boot 기본값(StringSerializer)이 걸려
#   "Can t convert value of class EventEnvelope" 로 발행이 실패한다.
#   DLT 발행도 같은 템플릿을 쓰기 때문에 함께 깨진다. (2026-08-27 실제로 겪음)
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JacksonJsonSerializer

# ---------------------------------------------------------------------------
# 청구 연동 (검사결과 확정 -> 수납, BillingChargeProducer)
#
# ⚠ feeCodeMapping 은 앱 기동 시점에 한 번만 읽힌다(@ConfigurationProperties).
#   매핑을 추가/수정하면 재기동해야 반영된다 — CommonCodeCache 처럼 주기적으로
#   갱신되지 않는다.
# ---------------------------------------------------------------------------

# 발생서비스코드 — 확정값. admin SYSTEM_SOURCE_CD 그룹의 "05=검사시스템" (2026-09-09 확정)
app.billing.source-service-code=0abe63e3-0425-4259-9ba3-21d7323aae20

# 검사항목코드(labItemCode) -> 수가코드(feeCode) 매핑.
# ⚠ 2026-09-09 기준 실제 매핑표 미확정. 아래 값은 테스트용 임시값이다(FEE002 로 통일).
#   수납팀에서 검사항목코드-수가코드 매핑표를 받으면 이 설정값만 교체한다.
#   코드 구조는 이미 확정된 것이므로 값 교체 외 로직 변경은 불필요하다.
app.billing.fee-code-mapping.01=FEE002
app.billing.fee-code-mapping.02=FEE002
